# 网站部署指南

您的网站已经上传到GitHub，现在可以通过以下几种方式让别人访问：

## 🚀 推荐方案：Vercel（最简单，推荐）

Vercel 是专门为前端项目设计的部署平台，完全免费，支持自动部署。

### 步骤：

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add New Project"
   - 选择您的 GitHub 仓库
   - 点击 "Import"

3. **配置项目**
   - Framework Preset: 选择 "Vite"
   - Root Directory: 保持默认（./）
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

4. **部署**
   - 点击 "Deploy"
   - 等待几分钟，部署完成后会得到一个网址（如：`your-project.vercel.app`）

5. **自动部署**
   - 以后每次推送到 GitHub，Vercel 会自动重新部署
   - 完全免费，无需任何配置

### 优点：
- ✅ 完全免费
- ✅ 自动部署（每次 push 自动更新）
- ✅ 全球 CDN 加速
- ✅ 支持自定义域名
- ✅ 零配置

---

## 🌐 方案二：Netlify（同样简单）

Netlify 也是优秀的免费部署平台。

### 步骤：

1. **访问 Netlify**
   - 打开 https://www.netlify.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择您的 GitHub 仓库
   - 点击 "Import"

3. **配置构建**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

4. **完成**
   - 部署完成后会得到一个网址（如：`your-project.netlify.app`）

### 优点：
- ✅ 完全免费
- ✅ 自动部署
- ✅ 全球 CDN
- ✅ 支持自定义域名

---

## 📄 方案三：GitHub Pages（需要配置）

GitHub Pages 是 GitHub 自带的静态网站托管服务。

### 步骤：

1. **修改 vite.config.js**
   
   在项目根目录的 `vite.config.js` 文件中添加 `base` 配置：
   
   ```javascript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   
   export default defineConfig({
     plugins: [react()],
     base: '/your-repo-name/',  // 替换为您的仓库名称
     server: {
       port: 3000,
       open: true
     }
   })
   ```

2. **安装 GitHub Pages 部署插件**
   
   ```bash
   npm install --save-dev gh-pages
   ```

3. **修改 package.json**
   
   在 `scripts` 中添加：
   
   ```json
   {
     "scripts": {
       "dev": "vite",
       "build": "vite build",
       "preview": "vite preview",
       "deploy": "npm run build && gh-pages -d dist"
     }
   }
   ```

4. **部署**
   
   ```bash
   npm run deploy
   ```

5. **启用 GitHub Pages**
   - 在 GitHub 仓库中，进入 Settings → Pages
   - Source 选择 "gh-pages" 分支
   - 保存后，网站地址为：`https://your-username.github.io/your-repo-name/`

### 注意事项：
- ⚠️ 需要修改 `vite.config.js` 中的 `base` 路径
- ⚠️ 每次更新需要运行 `npm run deploy`
- ⚠️ 如果仓库是私有的，GitHub Pages 需要付费

---

## 🎯 快速对比

| 方案 | 难度 | 自动部署 | 自定义域名 | 推荐度 |
|------|------|----------|------------|--------|
| Vercel | ⭐ 非常简单 | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| Netlify | ⭐ 非常简单 | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| GitHub Pages | ⭐⭐⭐ 需要配置 | ❌ | ✅ | ⭐⭐⭐ |

---

## 📝 推荐操作步骤

**如果您想快速部署，建议使用 Vercel：**

1. 访问 https://vercel.com
2. 用 GitHub 登录
3. 导入您的仓库
4. 点击 Deploy
5. 完成！网站即可访问

**整个过程大约 5 分钟，完全免费！**

---

## 🔧 本地构建测试

在部署前，您可以先在本地测试构建：

```bash
# 安装依赖（如果还没安装）
npm install

# 构建项目
npm run build

# 预览构建结果
npm run preview
```

如果本地预览正常，部署到 Vercel 或 Netlify 应该也没问题。

---

## ❓ 常见问题

### Q: 部署后页面显示空白？
A: 检查 `vite.config.js` 中的 `base` 配置是否正确（GitHub Pages 需要设置）

### Q: 图片或资源加载失败？
A: 确保所有资源路径使用相对路径，不要使用绝对路径

### Q: 路由不工作？
A: 如果是单页应用（SPA），需要在部署平台配置重定向规则（Vercel 和 Netlify 会自动处理）

### Q: 如何更新网站？
A: 
- Vercel/Netlify: 直接 push 到 GitHub，会自动更新
- GitHub Pages: 运行 `npm run deploy`

---

## 🎉 完成！

部署完成后，您就可以将网站链接分享给其他人访问了！

如有问题，请查看：
- Vercel 文档：https://vercel.com/docs
- Netlify 文档：https://docs.netlify.com
- GitHub Pages 文档：https://docs.github.com/pages

